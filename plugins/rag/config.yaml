# RAG 插件自有配置
# 环境变量 RAG_API_URL / RAG_API_KEY 优先级高于此文件

api_url: ""              # 外部 RAG 服务地址，例: "https://your-rag-service.com/api/search"
api_key: ""              # 鉴权 Key
timeout: 30              # 请求超时（秒）
default_top_k: 3         # 默认返回结果数量
